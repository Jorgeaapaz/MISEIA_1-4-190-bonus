stages:
  - test

# ── Stage 1: Lint + Unit Tests ───────────────────────────────────────────────
test:
  stage: test
  image: node:20-alpine
  tags:
    - cloudrun
  script:
    - npm ci
    - npm run lint
    - npm run test
  variables:
    JWT_SECRET: "test-secret-32-chars-minimum-ok!!"
